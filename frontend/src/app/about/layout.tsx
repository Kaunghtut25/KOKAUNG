import { Metadata } from "next";
export const metadata: Metadata = {
  title: "About A9 Global Travels & Tours",
  description: "Learn about A9 Global Travels & Tours — IATA-accredited luxury travel agency in Myanmar since 2015.",
};
export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return children;
}
