import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Myanmar Tour Packages & Guided Tours",
  description: "Explore Myanmar with A9 Global Travels. Guided tours to Bagan, Inle Lake, Mandalay, Yangon and beyond. Custom itineraries available.",
};
export default function ToursLayout({ children }: { children: React.ReactNode }) {
  return children;
}
