import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Contact A9 Global Travels",
  description: "Get in touch with A9 Global Travels & Tours. Phone, email, office address in Yangon, Myanmar.",
};
export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
